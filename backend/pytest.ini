[pytest]
addopts = --cov=app --cov-report=term-missing --cov-fail-under=95
asyncio_mode = auto
# la suite usa solo tests como funciones; asi pytest no intenta "colectar" los
# schemas Pydantic TestCase/TestPlan/TestResult como clases de test.
python_classes =
